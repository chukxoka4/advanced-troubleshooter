{
  "tenantId": "team-alpha",
  "displayName": "Team Alpha (prototype test tenant)",
  "repos": [
    {
      "owner": "chukxoka4",
      "name": "advanced-troubleshooter",
      "description": "The assistant repo itself — useful for self-referential test queries during the prototype",
      "githubToken": "${TEAM_ALPHA_GH_TOKEN}",
      "defaultBranch": "main"
    },
    {
      "owner": "chukxoka4",
      "name": "exchange-api",
      "description": "Older public repo included in the prototype tenant so Phase 1 grounding can be verified against a repo GitHub code search has already indexed",
      "githubToken": "${TEAM_ALPHA_GH_TOKEN}",
      "defaultBranch": "main"
    },
    {
      "owner": "chukxoka4",
      "name": "scraping-spider",
      "description": "Older public repo included in the prototype tenant so Phase 1 grounding can be verified against a repo GitHub code search has already indexed",
      "githubToken": "${TEAM_ALPHA_GH_TOKEN}",
      "defaultBranch": "main"
    },
    {
      "owner": "chukxoka4",
      "name": "check-the-number",
      "description": "Older public repo included in the prototype tenant so Phase 1 grounding can be verified against a repo GitHub code search has already indexed",
      "githubToken": "${TEAM_ALPHA_GH_TOKEN}",
      "defaultBranch": "main"
    }
  ],
  "issueConfig": {
    "targetRepo": "chukxoka4/advanced-troubleshooter",
    "writeToken": "${TEAM_ALPHA_ISSUE_TOKEN}",
    "templates": ["bug_report", "feature_request"]
  },
  "ai": {
    "provider": "openai",
    "model": "gpt-4o",
    "apiKey": "${TEAM_ALPHA_LLM_API_KEY}",
    "dailySpendCapUsd": 10
  },
  "systemPrompt": "You are a support assistant for the advanced-troubleshooter prototype. Explain how the code works and where to look; do not suggest direct code edits. Reference file paths precisely.",
  "allowedOrigins": [],
  "allowedUsers": [],
  "rateLimits": {
    "questionsPerMinute": 30,
    "issuesPerHour": 20
  }
}
